{
  "version": "1.0.0",
  "name": "lex-integrity",
  "description": "Local AI Policy & Regulatory Compliance Matrix",
  "scripts": {
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm start\"",
    "build": "cd server && npm run build && cd client && npm run build",
    "test": "cd server && npm run test && cd client && npm test",
    "lint": "cd server && npx eslint src && cd client && npx eslint src"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "node-fetch": "^3.3.2"
  }
}
